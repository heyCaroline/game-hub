import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <Input
      borderRadius="20px"
      placeholder="Search games..."
      variant="filled"
    ></Input>
  );
};

export default SearchInput;
